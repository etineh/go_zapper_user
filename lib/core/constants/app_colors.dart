import 'package:flutter/material.dart';

class AppColors {
  // Primary colors (Green from the design)
  static const Color primary = Color(0xFF03C068);
  static const Color primaryDark = Color(0xFF00B856);
  static const Color primaryLight = Color(0xFF1CC677);

  // Background colors
  static const Color background = Color(0xFFF6FBEF); // 38% opacity
  static const Color white = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);

  // Border colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderFocused = Color(0xFF00D968);

  // Status colors
  static const Color success = Color(0xFF00D968);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);
  static const Color info = Color(0xFF29B6F6);

  // Social login colors
  static const Color google = Color(0xFFDB4437);
  static const Color facebook = Color(0xFF4267B2);
  static const Color apple = Color(0xFF000000);

  // Overlay colors
  static const Color overlay = Color(0x80000000);
  static const Color disabled = Color(0xFFBDBDBD);
}
